plugins {
    id 'java'
    id 'maven'
    id 'maven-publish'
}

group 'org.medibloc.vc'
version '0.0.1'

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.fasterxml.jackson.datatype', name: 'jackson-datatype-joda', version: '2.9.8'
    compile group: 'org.projectlombok', name: 'lombok', version: '1.18.8'
    compile group: 'com.nimbusds', name: 'nimbus-jose-jwt', version: '9.1'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

publishing {
    repositories {
        maven {
            url = uri("https://maven.pkg.github.com/medibloc/vc")
            credentials {
                username = System.getenv("GPR_USER")
                password = System.getenv("GPR_API_KEY")
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            from(components.java)
        }
    }
}
